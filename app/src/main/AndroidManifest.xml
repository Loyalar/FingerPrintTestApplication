<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.loyalar.fingerprinttestapplication"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_action_fingerprint"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_action_fingerprint"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".PostFormActivity">
        </activity>
        <activity android:name=".FingerprintChoiceActivity" android:label="Fingerprint">
        </activity>
    </application>

</manifest>